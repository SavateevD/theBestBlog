@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IEnumerable<theBestBlog.Domain.Objects.Tag> tags)
{
foreach (var tag in tags)
{
        <div class="tags-div">
            @theBestBlog.WebUI.HtmlHelpers.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}}