@model IList<theBestBlog.Domain.Objects.Post>


<div class="card my-4">
    <h5 class="card-header">Latest Posts</h5>
    <div class="card-body">
        <div class="row">
            <div class="col-lg-6">
                @if (Model.Count > 0)
                {
                    <ul class="list-unstyled mb-0">
                        @foreach (var post in Model)
                        {
                            <li>@Html.PostLink(post)</li>
                        }
                    </ul>
                }
                else
                {
                    <p>No categories found!</p>
                }

            </div>
        </div>
    </div>
</div>